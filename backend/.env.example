# ShadowPartner Backend Configuration

# --- Whisper Settings ---
# Device to run Whisper on. 
# Options: "cuda" (for GPU), "cpu", or leave empty to auto-detect.
WHISPER_DEVICE=

# Use fp16 (half-precision) for inference.
# Set to "true" for faster inference on supported GPUs.
# Set to "false" if you encounter "NaN" errors or crashes.
WHISPER_FP16=false

# Whisper model size
# Options: tiny, base, small, medium, large
WHISPER_MODEL_SIZE=base


# --- External API Keys ---
# Google Gemini API Key for translation
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini Model ID
GEMINI_MODEL_ID=gemini-3-flash-preview

# Batch translation chunk size
TRANSLATE_BATCH_CHUNK_SIZE=50

# Subtitle similarity warning threshold (0.0 to 1.0)
# Warning will be shown if similarity is below this value
SUBTITLE_SIMILARITY_THRESHOLD=0.1


# --- Network Settings ---
# HTTP Proxy (optional)
# Example: http://127.0.0.1:7890
HTTP_PROXY=
HTTPS_PROXY=
